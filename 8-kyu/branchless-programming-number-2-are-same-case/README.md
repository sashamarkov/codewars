<h2><a href=https://www.codewars.com/kata/69747336aea32348dd1b8dcb/train/nasm target="_blank">Branchless Programming #2: Are Same Case</a></h2><h3>8 kyu</h3><p>Write a function that takes two English letters and returns a boolean (<code>0</code> or <code>1</code>) indicating whether they are either both uppercase or both lowercase.</p><pre><code class="language-c"><span class="cm-variable">are_same_case</span>(<span class="cm-string">'a'</span>, <span class="cm-string">'b'</span>) <span class="cm-operator">=</span> <span class="cm-atom">true</span><span class="cm-variable">are_same_case</span>(<span class="cm-string">'A'</span>, <span class="cm-string">'b'</span>) <span class="cm-operator">=</span> <span class="cm-atom">false</span><span class="cm-variable">are_same_case</span>(<span class="cm-string">'a'</span>, <span class="cm-string">'B'</span>) <span class="cm-operator">=</span> <span class="cm-atom">false</span><span class="cm-variable">are_same_case</span>(<span class="cm-string">'A'</span>, <span class="cm-string">'B'</span>) <span class="cm-operator">=</span> <span class="cm-atom">true</span></code></pre><h1 id="constraints">Constraints</h1><ol><li>The submission should be <strong>at most 5 lines of code</strong>, assuming 1 <code>global</code> directive at the top and 1 instruction per line.</li><li>The input is always going to be within <code>[a-zA-Z]</code>.</li><li>You cannot cheat by declaring data (via <code>Dx</code>), using macros/preprocessing (<code>%</code> and <code>\</code> characters), calling <code>extern</code> functions, or manipulating stack (<code>rsp</code>, <code>rbp</code>, <code>push</code>, and <code>pop</code>).</li></ol>