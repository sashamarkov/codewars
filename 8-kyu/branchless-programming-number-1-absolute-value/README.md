<h2><a href=https://www.codewars.com/kata/69744ef7a4c906f08c054dcb/train/nasm target="_blank">Branchless Programming #1: Absolute Value</a></h2><h3>8 kyu</h3><p>Write a function that takes a 64-bit integer <code>x</code> and returns its absolute value (<code>|x|</code>).</p><pre><code class="language-c"><span class="cm-variable">absolute</span>(<span class="cm-operator">-</span><span class="cm-number">26</span>) <span class="cm-operator">=</span> <span class="cm-number">26</span><span class="cm-variable">absolute</span>(<span class="cm-number">0</span>) <span class="cm-operator">=</span> <span class="cm-number">0</span><span class="cm-variable">absolute</span>(<span class="cm-operator">+</span><span class="cm-number">26</span>) <span class="cm-operator">=</span> <span class="cm-number">26</span></code></pre><h1 id="constraints">Constraints</h1><ol><li>The submission should be <strong>at most 5 lines of code</strong>, assuming 1 <code>global</code> directive at the top and 1 instruction per line.</li><li>The input will never overflow.</li><li>You cannot cheat by declaring data (via <code>Dx</code>), using macros/preprocessing (<code>%</code> and <code>\</code> characters), calling <code>extern</code> functions, or manipulating stack (<code>rsp</code>, <code>rbp</code>, <code>push</code>, and <code>pop</code>).</li></ol>