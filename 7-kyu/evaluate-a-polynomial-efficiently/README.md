<h2><a href=https://www.codewars.com/kata/6a2c623fb42a4ee098b2c36b/train/python target="_blank">Evaluate a Polynomial Efficiently</a></h2><h3>7 kyu</h3><p>Given an array of coefficients representing a polynomial and a value <code>x</code>, compute the value of the polynomial.The coefficients are ordered from the highest degree term to the constant term.</p><p>For Example: <code>[2,3,4]</code></p><p>Represent: <code>2x² + 3x + 4</code></p><h4 id="task">Task:</h4><p>Implement a function that evaluates the polynomial at the given value of <code>x</code>.</p><h4 id="examples">Examples:</h4><p><code>evaluate([2, 3, 4], 2) == 18</code>because <code>2·2² + 3·2 + 4 = 8 + 6 + 4 = 18</code>.</p><h4 id="notes">Notes:</h4><ul><li>The polynomial may contain a large number of coefficients.</li><li>Coefficients may be positive, negative, or zero.</li><li><code>x</code> may be positive, negative, or zero.</li><li>Your solution should be efficient enough to handle large inputs.</li></ul>