<h2><a href=https://www.codewars.com/kata/6a54d3e91e9fb16ca31cc912/train/python target="_blank">Find The Corrupted Hex Byte</a></h2><h3>7 kyu</h3><h2 id="task">Task</h2><p>A hexadecimal dump should contain only valid hexadecimal bytes.</p><p>A valid byte:</p><ul><li>consists of exactly two characters.</li><li>contains only hexadecimal digits (<code>0-9</code>, <code>A-F</code>).</li><li>uses uppercase letters only.</li></ul><p>Return the index of the first corrupted byte.</p><p>If every byte is valid, return <code>-1</code>.</p><h3 id="examples">Examples</h3><p>find_corrupted_byte(["48", "65", "6C", "6C", "6F"])</p><h1 id="-1">-1</h1><p>find_corrupted_byte(["48", "65", "6G", "6C", "6F"])</p><h1 id="2">2</h1><p>find_corrupted_byte(["48", "6", "6C"])</p><h1 id="1">1</h1><p>find_corrupted_byte(["48", "6c", "6F"])</p><h1 id="1-1">1</h1><h3 id="notes">Notes</h3><ul><li>The input is a list of strings.</li><li>An empty list is considered valid.</li><li>If multiple corrupted bytes exist, return the index of the first corrupted byte.</li><li>Lowercase and mixed-case hexadecimal letters are invalid.</li></ul>