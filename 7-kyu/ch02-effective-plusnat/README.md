<h2><a href=https://www.codewars.com/kata/5f874da45e9ce2001988c6bd/train/haskell target="_blank">Ch02 - effective plusNat</a></h2><h3>7 kyu</h3><p>Have a look at these definitions of <code>Nat</code> type and <code>plusNat</code> function:</p><pre><code class="language-haskell"><span class="cm-keyword">data</span> <span class="cm-variable-2">Nat</span> <span class="cm-keyword">=</span> <span class="cm-variable-2">Zero</span> <span class="cm-variable">|</span> <span class="cm-variable-2">Succ</span> <span class="cm-variable-2">Nat</span> <span class="cm-keyword">deriving</span> (<span class="cm-builtin">Show</span>, <span class="cm-builtin">Eq</span>)<span class="cm-variable">plusNat</span> <span class="cm-keyword">::</span> <span class="cm-variable-2">Nat</span> <span class="cm-keyword">-&gt;</span> <span class="cm-variable-2">Nat</span> <span class="cm-keyword">-&gt;</span> <span class="cm-variable-2">Nat</span><span class="cm-variable">plusNat</span> <span class="cm-variable">x</span> <span class="cm-variable-2">Zero</span> <span class="cm-keyword">=</span> <span class="cm-variable">x</span><span class="cm-variable">plusNat</span> <span class="cm-variable">x</span> (<span class="cm-variable-2">Succ</span> <span class="cm-variable">y</span>) <span class="cm-keyword">=</span> <span class="cm-variable-2">Succ</span> (<span class="cm-variable">x</span> `<span class="cm-variable">plusNat</span>` <span class="cm-variable">y</span>)</code></pre><p>The problem is that the number of recursive calls to the <code>plusNat</code> function depends on the value of the second argument. If we want to add one to a hundred, then the order of the arguments will significantly affect the speed of the calculation.</p><p>Your task is to write a more optimal version of the <code>plusNat</code> function.</p><p>Warn: only one test from <code>Problem illustration</code> section should fail, otherwise <strong>all broken</strong> and this kata should be skipped</p><p><a href="https://anton-k.github.io/ru-haskell-book/book/2.html#%D1%83%D0%BF%D1%80%D0%B0%D0%B6%D0%BD%D0%B5%D0%BD%D0%B8%D1%8F" data-turbolinks="false" target="_blank">Original description (ru)</a></p>