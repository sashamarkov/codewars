<h2><a href=https://www.codewars.com/kata/6a19ce85283166ed5c3dda8c/train/python target="_blank">Moser Streaming</a></h2><h3>7 kyu</h3><h1 id="moser-streaming">Moser Streaming</h1><p>In geometry, if you place points on a circle and connect every pair of points with straight lines, the circle becomes divided into regions.</p><p>The maximum number of regions created follows the famous Moser circle sequence:</p><pre><code class="language-text">1, 2, 4, 8, 16, 31, 57, 99, 163, 256, ...</code></pre><h2 id="task">Task</h2><p>Your task is to create an <strong>infinite generator</strong> that yields the values of this sequence one by one.</p><p>The sequence begins at <code>n = 1</code>.</p><p>Implement a generator function:</p><pre><code class="language-python"><span class="cm-keyword">def</span> <span class="cm-def">moser</span>():</code></pre><p>Which yields the values in the Moser circle sequence indefinitely.</p><h2 id="example">Example</h2><pre><code class="language-python"><span class="cm-keyword">for</span> <span class="cm-variable">i</span> <span class="cm-keyword">in</span> <span class="cm-variable">moser</span>():    <span class="cm-builtin">print</span>(<span class="cm-variable">i</span>)<span class="cm-comment"># prints 1, 2, 4, 8, 16, 31, 57, 99, 163, 256, 386, 562, 794, 1093...</span></code></pre>