<h2><a href=https://www.codewars.com/kata/5ac3c7ca034a0b3227001ad5/train/cpp target="_blank">FromPascalToCpp</a></h2><h3>7 kyu</h3><p>One day when I was little, I had to write a cycle, but I did not know how to do it, and I did not have internet. So I tried to find a solution in the book. I only had a <code>Delphi</code> book (<code>Pascal</code>). And I found the piece of code that suits me:</p><pre><code>begin    repeat        Sum := Sum + start;        start := start + 1;    until(start&gt;=finish);end;</code></pre><p>And put it in the code. But it does not work. I found out that instead of <code>:=</code> on <code>C++</code> you need to put <code>=</code>. And I replaced it, but it still did not work. Could you help me to adapt the code to <code>C++</code>. But I have one condition: You can not change the code that is written in <code>Pascal</code>.In the test, the code should also work.</p>