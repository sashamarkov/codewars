<h2><a href=https://www.codewars.com/kata/637d1d6303109e000e0a3116/train/python target="_blank">Get match by pattern</a></h2><h3>6 kyu</h3><p>You are given a string with three lowercase letters ( <code>pattern</code> ).</p><p><strong>Your Task</strong></p><p>Implement a function <code>find_matched_by_pattern(pattern)</code> that returns a predicate function, testing a string input and returning <code>true</code> if the string is matching the pattern, <code>false</code> otherwise.</p><p>A word is considered a match for a given pattern if the first occurrence of each letter of the pattern is found in the same order in the word. If a character in the pattern is duplicated, the same logic applies further.</p><p>The pattern will always be a string of size 3.</p><p>Example of use:</p><pre><code class="language-python"><span class="cm-variable">predicate</span> <span class="cm-operator">=</span> <span class="cm-variable">find_matched_by_pattern</span>(<span class="cm-string">'acs'</span>)<span class="cm-variable">predicate</span>(<span class="cm-string">'access'</span>) <span class="cm-comment"># True</span><span class="cm-variable">predicate</span>(<span class="cm-string">'sacrifice'</span>) <span class="cm-comment"># False </span></code></pre><p>Examples of inputs/outputs:</p><pre><code>Pattern:  Word:     Match:acs       access    true          ascces    false          scares    falsevvl       veturvel  true          velivel   falsebmb       bomb      true          babyboom  false</code></pre>