<h2><a href=https://www.codewars.com/kata/5e98a87ce8255200011ea60f/train/python target="_blank">field chained HTML formatting </a></h2><h3>5 kyu</h3><p>We want to create an object with methods for various HTML elements: <code>div</code>, <code>p</code>, <code>span</code> and <code>h1</code> for the sake of this Kata.</p><p>These methods will wrap the passed-in string in the tag associated with each.</p><pre style="display: none;"><code class="language-javascript"><span class="cm-variable">Format</span>.<span class="cm-property">div</span>(<span class="cm-string">"foo"</span>); <span class="cm-comment">// returns "&lt;div&gt;foo&lt;/div&gt;"</span><span class="cm-variable">Format</span>.<span class="cm-property">p</span>(<span class="cm-string">"bar"</span>); <span class="cm-comment">// returns "&lt;p&gt;bar&lt;/p&gt;"</span><span class="cm-variable">Format</span>.<span class="cm-property">span</span>(<span class="cm-string">"fiz"</span>); <span class="cm-comment">// returns "&lt;span&gt;fiz&lt;/span&gt;"</span><span class="cm-variable">Format</span>.<span class="cm-property">h1</span>(<span class="cm-string">"buz"</span>); <span class="cm-comment">// returns "&lt;h1&gt;buz&lt;/h1&gt;"</span></code></pre><pre><code class="language-python"><span class="cm-builtin">format</span>.<span class="cm-property">div</span>(<span class="cm-string">"foo"</span>)  <span class="cm-comment"># returns "&lt;div&gt;foo&lt;/div&gt;"</span><span class="cm-builtin">format</span>.<span class="cm-property">p</span>(<span class="cm-string">"bar"</span>)  <span class="cm-comment"># returns "&lt;p&gt;bar&lt;/p&gt;"</span><span class="cm-builtin">format</span>.<span class="cm-property">span</span>(<span class="cm-string">"fiz"</span>)  <span class="cm-comment"># returns "&lt;span&gt;fiz&lt;/span&gt;"</span><span class="cm-builtin">format</span>.<span class="cm-property">h1</span>(<span class="cm-string">"buz"</span>)  <span class="cm-comment"># returns "&lt;h1&gt;buz&lt;/h1&gt;"</span></code></pre><p>We also want to be able to add additional formatting by chaining our methods together.</p><pre style="display: none;"><code class="language-javascript"><span class="cm-variable">Format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span>(<span class="cm-string">"FooBar"</span>);<span class="cm-comment">// "&lt;div&gt;&lt;h1&gt;FooBar&lt;/h1&gt;&lt;div&gt;"</span><span class="cm-variable">Format</span>.<span class="cm-property">div</span>.<span class="cm-property">p</span>.<span class="cm-property">span</span>(<span class="cm-string">"FizBuz"</span>);<span class="cm-comment">// "&lt;div&gt;&lt;p&gt;&lt;span&gt;FizBuz&lt;/span&gt;&lt;/p&gt;&lt;/div&gt;"</span></code></pre><pre><code class="language-python"><span class="cm-builtin">format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span>(<span class="cm-string">"FooBar"</span>)<span class="cm-comment"># "&lt;div&gt;&lt;h1&gt;FooBar&lt;/h1&gt;&lt;div&gt;"</span><span class="cm-builtin">format</span>.<span class="cm-property">div</span>.<span class="cm-property">p</span>.<span class="cm-property">span</span>(<span class="cm-string">"FizBuz"</span>)<span class="cm-comment"># "&lt;div&gt;&lt;p&gt;&lt;span&gt;FizBuz&lt;/span&gt;&lt;/p&gt;&lt;/div&gt;"</span></code></pre><p>and so on, as deep as we care to use.</p><p>Multiple arguments should be concatenated and wrapped in the correct HTML formatting.</p><pre style="display: none;"><code class="language-javascript"><span class="cm-variable">Format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span>(<span class="cm-string">"Foo"</span>, <span class="cm-string">"Bar"</span>);<span class="cm-comment">// "&lt;div&gt;&lt;h1&gt;FooBar&lt;/h1&gt;&lt;/div&gt;"</span></code></pre><pre><code class="language-python"><span class="cm-builtin">format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span>(<span class="cm-string">"Foo"</span>, <span class="cm-string">"Bar"</span>)<span class="cm-comment"># "&lt;div&gt;&lt;h1&gt;FooBar&lt;/h1&gt;&lt;/div&gt;"</span></code></pre><p>We should be able to store the created methods and reuse them.</p><pre style="display: none;"><code class="language-javascript"><span class="cm-keyword">var</span> <span class="cm-def">wrapInDiv</span> <span class="cm-operator">=</span> <span class="cm-variable">Format</span>.<span class="cm-property">div</span>;<span class="cm-variable">wrapInDiv</span>(<span class="cm-string">"Foo"</span>);   <span class="cm-comment">// "&lt;div&gt;Foo&lt;/div&gt;"</span><span class="cm-variable">wrapInDiv</span>.<span class="cm-property">p</span>(<span class="cm-string">"Bar"</span>); <span class="cm-comment">// "&lt;div&gt;&lt;p&gt;Bar&lt;/p&gt;&lt;/div&gt;"</span><span class="cm-keyword">var</span> <span class="cm-def">wrapInDivH1</span> <span class="cm-operator">=</span> <span class="cm-variable">Format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span>;<span class="cm-variable">wrapInDivH1</span>(<span class="cm-string">"Far"</span>); <span class="cm-comment">// "&lt;div&gt;&lt;h1&gt;Far&lt;/h1&gt;&lt;/div&gt;"</span><span class="cm-variable">wrapInDivH1</span>.<span class="cm-property">span</span>(<span class="cm-string">"Bar"</span>); <span class="cm-comment">// "&lt;div&gt;&lt;h1&gt;&lt;span&gt;Bar&lt;/span&gt;&lt;/h1&gt;&lt;/div&gt;"</span></code></pre><pre><code class="language-python"><span class="cm-variable">wrap_in_div</span> <span class="cm-operator">=</span> <span class="cm-builtin">format</span>.<span class="cm-property">div</span><span class="cm-variable">wrap_in_div</span>(<span class="cm-string">"Foo"</span>)    <span class="cm-comment"># "&lt;div&gt;Foo&lt;/div&gt;"</span><span class="cm-variable">wrap_in_div</span>.<span class="cm-property">p</span>(<span class="cm-string">"Bar"</span>)  <span class="cm-comment"># "&lt;div&gt;&lt;p&gt;Bar&lt;/p&gt;&lt;/div&gt;"</span><span class="cm-variable">wrap_in_div_h1</span> <span class="cm-operator">=</span> <span class="cm-builtin">format</span>.<span class="cm-property">div</span>.<span class="cm-property">h1</span><span class="cm-variable">wrap_in_div_h1</span>(<span class="cm-string">"Far"</span>)  <span class="cm-comment"># "&lt;div&gt;&lt;h1&gt;Far&lt;/h1&gt;&lt;/div&gt;"</span><span class="cm-variable">wrap_in_div_h1</span>.<span class="cm-property">span</span>(<span class="cm-string">"Bar"</span>)  <span class="cm-comment"># "&lt;div&gt;&lt;h1&gt;&lt;span&gt;Bar&lt;/span&gt;&lt;/h1&gt;&lt;/div&gt;"</span></code></pre><p>And finally, we should be able to nest calls.</p><pre style="display: none;"><code class="language-javascript"><span class="cm-variable">Format</span>.<span class="cm-property">div</span>(  <span class="cm-variable">Format</span>.<span class="cm-property">h1</span>(<span class="cm-string">"Title"</span>),   <span class="cm-variable">Format</span>.<span class="cm-property">p</span>(<span class="cm-string-2">`Paragraph with a ${</span> <span class="cm-variable">Format</span>.<span class="cm-property">span</span>(<span class="cm-string">'span'</span>) <span class="cm-string-2">}</span><span class="cm-string-2">.`</span>))<span class="cm-comment">// returns "&lt;div&gt;&lt;h1&gt;Title&lt;/h1&gt;&lt;p&gt;Paragraph with a &lt;span&gt;span&lt;/span&gt;.&lt;/p&gt;&lt;/div&gt;"</span></code></pre><pre><code class="language-python"><span class="cm-builtin">format</span>.<span class="cm-property">div</span>(    <span class="cm-builtin">format</span>.<span class="cm-property">h1</span>(<span class="cm-string">"Title"</span>),    <span class="cm-builtin">format</span>.<span class="cm-property">p</span>(<span class="cm-string">f"Paragraph with a </span>{<span class="cm-builtin">format</span>.<span class="cm-property">span</span>(<span class="cm-string">'span'</span>)}<span class="cm-string">."</span>))<span class="cm-comment"># returns "&lt;div&gt;&lt;h1&gt;Title&lt;/h1&gt;&lt;p&gt;Paragraph with a &lt;span&gt;span&lt;/span&gt;.&lt;/p&gt;&lt;/div&gt;"</span></code></pre>